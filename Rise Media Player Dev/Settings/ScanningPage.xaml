<Page
    x:Class="Rise.App.Settings.ScanningPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    xmlns:conv="using:Rise.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Rise.App.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Microsoft.Toolkit.Uwp.UI.Media"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:uc="using:Rise.App.UserControls"
    xmlns:viewmodels="using:Rise.App.ViewModels" xmlns:local1="using:Rise.App"
    d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    mc:Ignorable="d">
    <Grid>
        <uc:MicaTitleBar x:Name="AppTitleBar" VerticalAlignment="Top" ShowIcon="False" Title="" />
        <Grid Padding="20,48,20,12">
            <StackPanel>
                <TextBlock Text="Media scanning" Padding="0,0,0,12" FontSize="28" FontWeight="SemiBold" TextWrapping="WrapWholeWords"/>
                <uc:ExpanderControl x:Uid="/MediaLibrary/AutoScanMedia"
                               ExpanderStyle="Static"
                               Icon="&#xE12E;">
                    <ToggleSwitch IsOn="{x:Bind local1:App.SViewModel.AutoIndexingEnabled, Mode=TwoWay}" />
                </uc:ExpanderControl>
                
                <muxc:RadioButtons SelectionChanged="PeriodicScan_SelectionChanged" IsEnabled="{x:Bind local1:App.SViewModel.AutoIndexingEnabled, Mode=OneWay}" Margin="0,14,0,0" x:Name="PeriodicScan" SelectedIndex="3" Header="Scan periodically every:">
                    <x:String>Never</x:String>
                    <x:String>1 minute</x:String>
                    <x:String>5 minutes</x:String>
                    <x:String>10 minutes</x:String>
                    <x:String>30 minutes</x:String>
                    <x:String>1 hour</x:String>
                </muxc:RadioButtons>
            </StackPanel>
        </Grid>
    </Grid>

    
</Page>
