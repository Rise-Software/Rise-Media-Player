<Page
    x:Class="Rise.App.Views.SongLyricsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Rise.App.Views.Songs.Properties"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ListView x:Name="LyricsListView">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="x:String">
                <TextBlock Text="{x:Bind}" />
            </DataTemplate>
        </ListView.ItemTemplate>

        <ListView.Footer>
            <StackPanel
                Padding="16,4"
                Orientation="Vertical"
                Spacing="4">
                <TextBlock x:Name="CopyrightTextBlock" TextWrapping="Wrap" />
                <TextBlock Text="Powered by Musixmatch." />
            </StackPanel>
        </ListView.Footer>
    </ListView>
</Page>
