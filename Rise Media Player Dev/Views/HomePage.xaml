<Page
    x:Class="RMP.App.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RMP.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <muxc:InfoBar x:Name="ConnectServiceNotification"
                      Title="Want better recommendations?"
                      Message="Access curated playlists, radio stations, sync your personal playlists and even stream media. There's even more too. Connect a service like Spotify now."
                      Grid.Row="0">
            <muxc:InfoBar.ActionButton>
                <Button Content="Connect a streaming service"/>
            </muxc:InfoBar.ActionButton>

        </muxc:InfoBar>
        <Grid Grid.Row="1">
            <TextBlock TextWrapping="WrapWholeWords" Padding="0,20,0,0" Text="A song we think you'll love" FontFamily="Segoe UI Variable" FontWeight="SemiBold" FontSize="14"/>
            <TextBlock TextWrapping="WrapWholeWords" Padding="285,20,0,0" Text="Similar tracks" FontFamily="Segoe UI Variable" FontWeight="SemiBold" FontSize="14"/>
        </Grid>
    </Grid>
</Page>