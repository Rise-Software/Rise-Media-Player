<Page
    x:Class="Rise.App.Dialogs.WidgetsDialogContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:Rise.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:Rise.App.UserControls"
    xmlns:viewmodels="using:Rise.Data.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <conv:ResourceToString x:Key="StrToRes" />
    </Page.Resources>

    <ListView ItemsSource="{x:Bind MViewModel.Widgets}" SelectionMode="None">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="viewmodels:WidgetViewModel">
                <uc:ExpanderControl
                    Title="{x:Bind Title, Converter={StaticResource StrToRes}}"
                    Margin="0,2"
                    Description="{x:Bind Description, Converter={StaticResource StrToRes}}"
                    ExpanderStyle="Static">
                    <ToggleSwitch IsOn="{x:Bind Enabled, Mode=TwoWay}" />
                </uc:ExpanderControl>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
